{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Docker Next.js",
      "type": "node",
      "request": "attach",
      "port": 9229,
      "address": "localhost",
      "localRoot": "${workspaceFolder}",
      "remoteRoot": "/app", // Change to match your Docker container's WORKDIR
      "restart": true,
      "skipFiles": ["<node_internals>/**"]
    }
  ]
}
